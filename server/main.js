import { Meteor } from 'meteor/meteor';
import '../imports/api/games';
import '../imports/api/rounds';
import '../imports/api/words';

Meteor.startup(() => {
  // code to run on server at startup
});
